# Copy to .env and fill in your values. Do NOT commit .env to the repository.
TELEGRAM_TOKEN=put-your-telegram-bot-token-here
ANALYST_CHAT_ID=1057293934

# Proxy (if needed to reach Telegram)
USE_PROXY=true
PROXY_URL=socks5://127.0.0.1:10808

# LLM. false = run offline on the rule-based parser (handy for the defense).
USE_LLM=false
MISTRAL_API_KEY=put-your-mistral-key-here
LLM_BASE_URL=https://api.mistral.ai/v1
LLM_MODEL=mistral-small-latest

# Google Sheets (disabled by default)
USE_SHEETS=false
GOOGLE_CREDS_FILE=GoogleCreds.json

DEMO_MODE=true
COLLECTOR_MAX_RETRIES=2
REMINDER_AFTER_SEC=60
ESCALATE_AFTER_SEC=120
ANOMALY_THRESHOLD=0.5
